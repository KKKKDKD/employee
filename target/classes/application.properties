spring.mvc.view.prefix=/WEB-INF/view/
spring.mvc.view.suffix=.jsp
server.port=444

#This is for spring data +jpa + orm configuration 
spring.datasource.url = jdbc:mysql://localhost:3306/zaradb?createDatabaseIfNotExist=true
spring.datasource.username = root
spring.datasource.password = Rida@@8899
## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
#The simplest way to dump the queries to standard out 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
